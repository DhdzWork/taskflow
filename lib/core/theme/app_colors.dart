import 'package:flutter/material.dart';

class MyAppColors {
  final Color primaryApp = Colors.deepOrangeAccent;
  final Color secondaryApp = Colors.orange;
  final Color accentApp = Colors.amberAccent;
  final Color backgroundApp = Colors.grey[100]!; // Fondo general limpio
  final Color selectedWidget = const Color.fromARGB(255, 255, 163, 134).withOpacity(0.5);
  final Color titleColor = Colors.grey[900]!; // Negro suave
  final Color subtitleColor = Colors.grey[700]!; // Gris medio
  final Color descriptionColor = Colors.grey[600]!; // Gris suave
}
